# Nixpacks configuration for Railway
# Railway will auto-detect Python, but this ensures SQLite is available

[phases.setup]
nixPkgs = ["python3", "sqlite"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[start]
cmd = "python enhanced_scheduler.py"

